{
  "version": 2,
  "builds": [
    { "src": "index.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/whois/(?<domain>[^/]+)", "dest": "/api/whois/[domain].js?domain=$domain" },
    { "src": "/(.*)", "dest": "/index.js" }
  ]
}
